<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Know Us | Association des Scouts du Petit Ange</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    :root {
      --primary-color: #003366;
      --secondary-color: #f7f9fc;
      --accent-color: #3B82F6;
    }
    body {
      background-color: var(--secondary-color);
      font-family: 'Tajawal', Arial, sans-serif;
    }
    .navbar {
      background-color: var(--primary-color);
      color: white;
    }
    .navbar a {
      color: white;
      text-decoration: none;
    }
    .navbar a:hover {
      color: var(--accent-color);
    }
    .navbar .navbar-brand {
      font-weight: bold;
      font-size: 1.5rem;
      color: white;
    }
    .hero-section {
      background-color: var(--primary-color);
      color: white;
      padding: 60px 20px;
      text-align: center;
    }
    .hero-section h1 {
      font-size: 2.5rem;
      font-weight: bold;
    }
    .hero-section p {
      font-size: 1.2rem;
      margin-top: 15px;
    }
    .about-section {
      padding: 40px 20px;
      color: #333;
    }
    .about-section h2 {
      font-size: 2rem;
      font-weight: bold;
      color: var(--primary-color);
      text-align: center;
      margin-bottom: 30px;
    }
    .team-member {
      text-align: center;
      margin-bottom: 30px;
    }
    .team-member img {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      margin-bottom: 15px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .team-member h5 {
      font-size: 1.1rem;
      color: var(--primary-color);
      font-weight: bold;
    }
    .team-member p {
      font-size: 0.9rem;
      color: #555;
    }
  </style>
</head>
<body>
  <!-- Include Navbar -->
  <%- include('partials/navbar') %>

  <!-- Hero Section -->
  <div class="hero-section">
    <h1>Welcome to Association des Scouts du Petit Ange</h1>
    <p>Discover our story, our values, and the amazing people who make it all possible.</p>
  </div>

  <!-- About Section -->
  <div class="container about-section">
    <h2>About Us</h2>
    <p class="text-center">
      At Association des Scouts du Petit Ange, we are dedicated to empowering young minds and fostering a spirit of teamwork, leadership, and community service. 
      With years of experience and countless success stories, we take pride in our mission to make a positive impact.
    </p>
  </div>

  <!-- Include Footer -->
  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
