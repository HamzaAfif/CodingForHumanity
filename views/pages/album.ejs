<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= album.title %> | Association des Scouts du Petit Ange</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    :root {
      --primary-color: #003366;
      --secondary-color: #f7f9fc;
      --accent-color: #3B82F6;
    }
    body {
      background-color: var(--secondary-color);
      font-family: 'Tajawal', Arial, sans-serif;
    }
    .album-header {
      text-align: center;
      margin-bottom: 30px;
    }
    .album-title {
      color: var(--primary-color);
      font-size: 2.5rem;
      font-weight: bold;
    }
    .album-description {
      font-size: 1.2rem;
      color: #555;
    }
    .album-images img {
      width: 100%;
      border-radius: 10px;
      margin-bottom: 15px;
      transition: transform 0.3s ease-in-out;
    }
    .album-images img:hover {
      transform: scale(1.05);
    }
  </style>
</head>
<body>
  <!-- Navbar Include -->
  <%- include('partials/navbar') %>

  <!-- Main Content -->
  <div class="container my-5">
    <div class="album-header">
      <h1 class="album-title text-white"><%= album.title %></h1>
<p class="album-description text-white"><%= album.description %></p>

    </div>

    <div class="row album-images">
      <% if (images && images.length > 0) { %>
        <% images.forEach(image => { %>
          <div class="col-md-4 mb-3">
            <img src="/<%= image %>" alt="<%= album.title %> Image" class="img-fluid shadow-sm">
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-12 text-center">
          <p class="text-muted">No images available for this album.</p>
        </div>
      <% } %>
    </div>
  </div>

  <!-- Footer Include -->
  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
