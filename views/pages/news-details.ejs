<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= article.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f9f9f9; /* Neutral background */
      padding-top: 80px; /* Prevent navbar overlap */
    }

    .container {
      max-width: 800px;
      background: #ffffff; /* Clean white background for content */
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
      margin-bottom: 30px;
    }

    h1 {
      font-size: 2rem;
      font-weight: 600;
      color: #333;
      margin-bottom: 15px;
    }

    .article-date {
      font-size: 0.9rem;
      color: #6c757d; /* Muted gray for dates */
      margin-bottom: 20px;
    }

    .image-gallery {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
      justify-content: center;
      margin-bottom: 20px;
    }

    .image-gallery img {
      max-width: 100%;
      height: auto;
      border-radius: 5px;
      box-shadow: 0 1px 5px rgba(0, 0, 0, 0.1);
    }

    p {
      line-height: 1.6;
      color: #555;
      font-size: 1rem;
    }

    .btn-back {
      display: inline-block;
      background-color: #007bff;
      color: #fff;
      padding: 10px 20px;
      border-radius: 5px;
      text-decoration: none;
      font-weight: 500;
      text-align: center;
    }

    .btn-back:hover {
      background-color: #0056b3;
    }

    footer {
      background-color: #f8f9fa;
      text-align: center;
      padding: 10px 0;
      font-size: 0.9rem;
      color: #6c757d;
      border-top: 1px solid #e9ecef;
    }
  </style>
</head>
<body>
  <!-- Include Navbar -->
  <%- include('partials/navbar') %>

  <!-- Main Content -->
  <div class="container">
    <h1><%= article.title %></h1>
    <p class="article-date">Published on <%= article.date %></p>

    <!-- Image Gallery -->
    <% if (images.length > 0) { %>
      <div class="image-gallery">
        <% images.forEach(image => { %>
          <img src="<%= image.image_path %>" alt="Image for <%= article.title %>">
        <% }); %>
      </div>
    <% } else { %>
      <p class="text-muted text-center">No images available for this article.</p>
    <% } %>

    <!-- Article Content -->
    <p><%= article.full_text %></p>

    <!-- Back Button -->
    <div class="text-center mt-4">
      <a href="/" class="btn-back">Back to Home</a>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    <p>&copy; 2024 Your Website Name. All rights reserved.</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
