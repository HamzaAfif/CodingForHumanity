<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>المناهج | جمعية كشافة الملاك الصغير</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#003366',
            secondary: '#f7f9fc',
          },
          fontFamily: {
            sans: ['Tajawal', 'sans-serif'],
          },
        },
      },
    }
  </script>
  <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
</head>
<body class="bg-secondary font-sans">
  <!-- Header Section -->
  <header class="bg-primary text-white py-8">
    <div class="container mx-auto px-4">
      <div class="flex flex-col md:flex-row items-center justify-between">
        <h1 class="text-3xl md:text-4xl font-bold mb-4 md:mb-0">المناهج</h1>
        <img src="/path-to-your-logo.png" alt="شعار الجمعية" class="h-16 w-auto">
      </div>
    </div>
  </header>

  <!-- Curricula Section -->
  <main class="container mx-auto px-4 py-8">
    <% if (curricula && curricula.length > 0) { %>
      <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
        <% curricula.forEach(curriculum => { %>
          <div class="bg-white rounded-lg shadow-md overflow-hidden">
            <div class="p-6">
              <div class="flex items-center justify-between mb-4">
                <% if (curriculum.logo_path) { %>
                  <img src="<%= curriculum.logo_path %>" class="w-12 h-12 object-contain" alt="شعار المنهج">
                <% } else { %>
                  <div class="w-12 h-12 bg-gray-100 rounded-full flex items-center justify-center text-primary">
                    <i class="fas fa-book-open text-xl"></i>
                  </div>
                <% } %>
                <span class="bg-primary text-white text-sm px-2 py-1 rounded">
                  <%= curriculum.category %>
                </span>
              </div>
              <h2 class="text-xl font-bold text-primary mb-2"><%= curriculum.title %></h2>
              <p class="text-gray-600 mb-4"><%= curriculum.description %></p>
              <a href="#" class="text-primary font-semibold hover:underline">
                اقرأ المزيد
                <i class="fas fa-arrow-left mr-1"></i>
              </a>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="text-center text-gray-500 py-12">
        <i class="fas fa-book-open text-5xl mb-4"></i>
        <p class="text-xl">لا توجد مناهج لعرضها حاليا.</p>
      </div>
    <% } %>
  </main>

  <footer class="bg-primary text-white py-4 mt-8">
    <div class="container mx-auto px-4 text-center">
      <p>&copy; <%= new Date().getFullYear() %> جمعية كشافة الملاك الصغير. جميع الحقوق محفوظة.</p>
    </div>
  </footer>
</body>
</html>